#Partial which returns the Navigation Bar to render in sub-services. Centralising rendering logic.
GET        /navigation-items/trader/:ern             controllers.partials.NavBarPartialController.navBarItems(ern: String)